ROUNDS should only be defined once ( currently both in gui and console file)

add info box similar to console version to the gui version

use floating positioning within the gui instead of the absolute positions

when removing an account, update the GUI (message box can then be removed)

qt version sometimes crashes

allow deletion of accounts in qt version

remove tinker version

allow import / export of accounts in qt version
